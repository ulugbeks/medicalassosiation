<?php

return [
    'locales' => [
        'en', // English (default)
        'lv', // Latvian
    ],
    
    'default_locale' => 'en',
    
    // If you want to use a different locale when specific locales are not available
    'fallback_locale' => 'en',
    
    // Should application use the locale stored in a cookie?
    'use_property_fallback' => true,
];